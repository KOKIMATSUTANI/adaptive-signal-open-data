# Lightweight dependencies for GTFS/GTFS-RT ingestion only
# Specialized for data fetching, conversion, and storage

# Data compression and archive processing
zipfile36>=0.1.3

# Time and date processing
python-dateutil>=2.8.0

# Configuration file processing
pyyaml>=6.0

# Lightweight database (optional)
# sqlite3  # Standard library (no installation required)

# Logging and monitoring
structlog>=22.0.0
